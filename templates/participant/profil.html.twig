{% extends 'base.html.twig' %}

{% block title %}profil{% endblock %}

{% block body %}

    <div class="mt-5 container flex justify-center">
    {% if participant.id == app.user.id %}
      {{ form_errors(participantForm) }}
<form action="#" method="post">
{{ form_start(participantForm) }}
  <div class="space-y-12">
    <div class="border-b border-gray-900/10 pb-6">
      <h1 class="text-base font-semibold leading-7 text-gray-900">Mon profil</h1>
      
        <!--<div class="col-span-full">
          <label for="photo" class="block text-sm font-medium leading-6 text-gray-900">Photo</label>
          <div class="mt-2 flex items-center gap-x-3">
            <svg class="h-12 w-12 text-gray-300" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M18.685 19.097A9.723 9.723 0 0021.75 12c0-5.385-4.365-9.75-9.75-9.75S2.25 6.615 2.25 12a9.723 9.723 0 003.065 7.097A9.716 9.716 0 0012 21.75a9.716 9.716 0 006.685-2.653zm-12.54-1.285A7.486 7.486 0 0112 15a7.486 7.486 0 015.855 2.812A8.224 8.224 0 0112 20.25a8.224 8.224 0 01-5.855-2.438zM15.75 9a3.75 3.75 0 11-7.5 0 3.75 3.75 0 017.5 0z" clip-rule="evenodd" />
            </svg>
            <button type="button" class="rounded-md bg-white px-2.5 py-1.5 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50">Change</button>
          </div>
        </div>-->
      
    </div>
    <div class="border-b border-gray-900/10 pb-6">
      <div class="mt-10 grid grid-cols-1 gap-x-6 gap-y-8 sm:grid-cols-6">
        <div class="sm:col-span-3">
          <label for="{{ participantForm.pseudo.vars.id }}" class="block text-sm font-medium leading-6 text-gray-900">Pseudo</label>
          <div class="mt-2">
            {{ form_row(participantForm.pseudo, {'attr': {'class': 'px-2 py-1 border rounded focus:outline-none focus:border-blue-500'}, 'label': false}) }}     
          </div>
        </div>
        <div class="sm:col-span-3">
          <label for="{{ participantForm.email.vars.id }}" class="block text-sm font-medium leading-6 text-gray-900">Email</label>
          <div class="mt-2">
             {{ form_row(participantForm.email, {'attr': {'class': 'px-2 py-1 border rounded focus:outline-none focus:border-blue-500'}, 'label': false}) }}
          </div>
        </div>
        <div class="sm:col-span-3">
          <label for="{{ participantForm.prenom.vars.id }}" class="block text-sm font-medium leading-6 text-gray-900">Prénom</label>
          <div class="mt-2">
            {{ form_row(participantForm.prenom, {'attr': {'class': 'px-2 py-1 border rounded focus:outline-none focus:border-blue-500'}, 'label': false}) }}
          </div>
        </div>
        <div class="sm:col-span-3">
          <label for="{{ participantForm.nom.vars.id }}" class="block text-sm font-medium leading-6 text-gray-900">Nom</label>
          <div class="mt-2">
             {{ form_row(participantForm.nom, {'attr': {'class': 'px-2 py-1 border rounded focus:outline-none focus:border-blue-500'}, 'label': false}) }}
          </div>
        </div>
        <div class="sm:col-span-3">
          <label for="{{ participantForm.telephone.vars.id }}" class="block text-sm font-medium leading-6 text-gray-900">Téléphone</label>
          <div class="mt-2">
             {{ form_row(participantForm.telephone, {'attr': {'class': 'px-2 py-1 border rounded focus:outline-none focus:border-blue-500'}, 'label': false}) }}
          </div>
        </div>
        <div class="sm:col-span-3">
          <label for="{{ participantForm.campus.vars.id }}" class="block text-sm font-medium leading-6 text-gray-900">Campus</label>
          <div class="mt-2">
           {{ form_row(participantForm.campus, {'attr': {'class': 'px-2 py-1 border rounded focus:outline-none focus:border-blue-500'}, 'label': false}) }}
          </div>
        </div>
        <div class="sm:col-span-3">
          <label for="Password" class="block text-sm font-medium leading-6 text-gray-900">Mot de passe</label>
          <div class="mt-2">
           {{ form_row(participantForm.motPasse.first, {'attr': {'class': 'px-2 py-1 border rounded focus:outline-none focus:border-blue-500'},'label': false, 'required': false}) }}
          </div>
        </div>
        <div class="sm:col-span-3">
          <label for="Repeat Password" class="block text-sm font-medium leading-6 text-gray-900">Confirmation mot de passe</label>
          <div class="mt-2">
           {{ form_row(participantForm.motPasse.second, {'attr': {'class': 'px-2 py-1 border rounded focus:outline-none focus:border-blue-500'}, 'label': false, 'required': false}) }}
          </div>
        </div>
      </div>
    </div>
    
  </div>
  <div class="mt-6 flex items-center justify-end gap-x-6">
    <button type="reset" class="text-sm font-semibold leading-6 text-gray-900">Annuler</button>
    <button type="submit" class="rounded-md bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600">Enregistrer</button>
  </div>
 
   {{ form_end(participantForm) }}

   
</form>
    {% endif %}
     {% if participant.id != app.user.id %}



<section class="bg-blueGray-50">
<div class="w-full lg:w-12/12 px-4 mx-auto">
  <div class="relative flex flex-raw min-w-0 break-words bg-white  mb-6 shadow-xl rounded-lg mt-12 px-10">
    <div class="flexpx-6">
      <div class="flex flex-wrap justify-center">
        <div class="w-full px-4 flex justify-center">
          <div class="relative  rounded-full h-auto align-middle border-none absolute -m-16 -ml-20 lg:-ml-16 max-w-150-px">
            <img alt="..." src="{{ asset('img/avatar.png') }}" class="">
          </div>
        </div>
      </div>
      <div class="text-center mt-16 ">
        <h1 class="text-3xl font-bold leading-normal mb-2 text-blueGray-700 mb-2" style="letter-spacing: 0.1em;">
          {{participant.pseudo|capitalize }}
        </h1>
        <div class="flex justify-center">
        <div class="text-lg leading-normal mt-0 mb-2 text-blueGray-400 font-semibold mx-2">
          <span class="font-bold">Prénom : </span>{{participant.prenom|capitalize}}
        </div>
        <div class="text-lg leading-normal mt-0 mb-2 text-blueGray-400 font-semibold mx-2">
          <span class="font-bold">Nom : </span>{{participant.nom|capitalize}}
        </div></div>
        
        <div class="text-lg leading-normal mt-2 mb-2 text-blueGray-400 font-semibold">
          <span class="font-bold">Téléphone :  </span>{% if participant.telephone == null %}
            . . - . . - . . - . . - . .
          {% endif %}{{participant.telephone}}
        </div>
        <div class="text-lg leading-normal mt-2 mb-2 text-blueGray-400 font-semibold">
          <span class="font-bold">Email : </span>{{participant.email}}
        </div>
        <div class="text-lg leading-normal mt-2 mb-2 text-blueGray-400 font-semibold">
          <span class="font-bold">Campus : </span>{{participant.campus.nom|upper }}
          
        </div>
        
      </div>
      <div class="flex justify-center mx-10 mb-5">
  <a href="{{ path('app_main') }}" class="inline-block px-8 py-4 mt-4 text-sm font-semibold text-white bg-indigo-600 rounded hover:bg-indigo-500 focus:bg-indigo-700 focus:outline-none focus-visible:ring focus-visible:ring-indigo-500 focus-visible:ring-opacity-50">
    Retour
  </a>
</div>
    </div>
    
  </div>
  
</div>

</section>
     {% endif %}
</div>



{% endblock %}
